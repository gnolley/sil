set(SIL_COR_INCLUDES 
	"${ENGINE_DIR}/includes/"
)

set(SIL_COR_SOURCES
 "SilEngine.cpp")

add_library(${ENGINE_SRC_TARGET} STATIC ${SIL_COR_SOURCES})
target_include_directories(${ENGINE_SRC_TARGET} PUBLIC ${SIL_COR_INCLUDES} ${PLAYBOOK_INCLUDE_DIR} ${MEZZANINE_INCLUDE_DIR} ${CORE_INCLUDE_DIR})

target_link_libraries(${ENGINE_SRC_TARGET} PUBLIC ${PLAYBOOK_LIB_TARGET} ${MEZZANINE_LIB_TARGET} ${CORE_LIB_TARGET})
add_dependencies(${ENGINE_SRC_TARGET} ${BUILD_BUILTIN_ASSETS_TARGET})

setup_target(${ENGINE_SRC_TARGET})